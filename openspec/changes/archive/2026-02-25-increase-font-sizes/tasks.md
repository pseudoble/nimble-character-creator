## 1. UI Primitives

- [x] 1.1 Replace `text-sm` with `text-base` in button.tsx
- [x] 1.2 Replace `text-xs` with `text-sm` in label.tsx
- [x] 1.3 Replace `text-sm` with `text-base` in input.tsx
- [x] 1.4 Replace `text-sm` with `text-base` in textarea.tsx
- [x] 1.5 Replace `text-sm` with `text-base` in select.tsx
- [x] 1.6 Replace `text-xs` with `text-sm` in tooltip.tsx

## 2. Creator Components

- [x] 2.1 Replace `text-sm` → `text-base` and `text-xs` → `text-sm` in creator-shell.tsx
- [x] 2.2 Replace `text-xs` → `text-sm` in character-basics-form.tsx
- [x] 2.3 Replace `text-xs` → `text-sm` in ancestry-background-form.tsx
- [x] 2.4 Replace `text-xs` → `text-sm`, `text-[10px]` → `text-xs`, `text-lg` → `text-xl` in stats-skills-form.tsx
- [x] 2.5 Replace `text-sm` → `text-base`, `text-xs` → `text-sm`, `text-[10px]` → `text-xs` in languages-equipment-form.tsx

## 3. Sheet Components

- [x] 3.1 Replace `text-xs` → `text-sm`, `text-[10px]` → `text-xs`, `text-lg` → `text-xl`, `text-sm` → `text-base` in character-sheet.tsx
- [x] 3.2 Replace `text-xs` → `text-sm`, `text-lg` → `text-xl` in dice-overlay.tsx
- [x] 3.3 Replace `text-2xl` → `text-3xl` in character-sheet.tsx (gold display)

## 4. Verification

- [x] 4.1 Visual check: creator wizard renders without overflow or truncation
- [x] 4.2 Visual check: character sheet stat grids and skill lists fit correctly
- [x] 4.3 Visual check: dice overlay text is legible and properly sized
